<UserControl x:Class="DesktopApplication.Controls.InfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopApplication.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="260">
    <UserControl.Resources>
        <Style x:Key="info_card_text" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#06BEB6"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="48"/>
        </Style>
        <Style x:Key="info_card_subtext" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Border CornerRadius="20,20,20,20" BorderBrush="White" Background="White">
        <StackPanel>
            <TextBlock Style="{StaticResource info_card_text}" Text="{Binding Value}"></TextBlock>
            <TextBlock Style="{StaticResource info_card_subtext}" Text="{Binding Text}"></TextBlock>
        </StackPanel>
    </Border>
</UserControl>